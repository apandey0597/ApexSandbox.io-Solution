public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if(con.AccountId == opp.AccountId && opp.AccountId == acc.Id && acc.Id != null){
        return true;
    }else
    return false;
}