public Boolean isPrime(Integer num) {
    //code here
    Integer c=0;
    for(Integer i=2;i<num;i++){
        if(Math.mod(num,i)==0){
            c++;
        }
    }
    if(c>0)
    return false;
    else return true;
}