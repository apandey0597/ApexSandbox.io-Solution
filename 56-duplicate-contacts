public Boolean duplicateContacts(Contact c1, Contact c2) {
    //code here
    if(c1.Email != null && c1.Email.equalsIgnoreCase(c2.Email)){
        return true;
    }else if(c1.Phone != null && c1.Phone.equalsIgnoreCase(c2.Phone)){
        return true;
    }else {
        return false;
    }
}